#lang racket

; Faça uma função chamada area-retangulo que recebe dois argumentos, a largura e a altura de um
; retângulo, e calcula a sua área. Confira na janela de interações se a função funciona de acordo com os
; exemplos a seguir

(define (area-retangulo largura altura)
  (* largura altura))